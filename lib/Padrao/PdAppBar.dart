import 'package:flutter/material.dart';

AppBar AppBarPadrao(){
  return AppBar(
    backgroundColor: Color(0xff478ca0),
    actions: [
      IconButton(
          onPressed: () {}
          , icon: Icon(
          Icons.account_circle
      )
      )
    ],
  );
}